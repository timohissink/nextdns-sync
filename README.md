# nextdns-synctool
A single-file web app to sync, edit, and manage multiple NextDNS profiles — no install, no backend. Copy settings across profiles with a full diff preview, manage devices, inspect DNS logs, and export/import configs. Just open in browser with your API key.
